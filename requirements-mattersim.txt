streamlit==1.57.0
plotly
pymatgen==2025.10.7
ase==3.25.0 # 3.27.0 is not supported currently with MatterSim
#phonopy==2.40.0
phonopy==2.41.0
py3Dmol==2.5.0

psutil
gputil

torch
# mattersim>=1.2.3 adds a phono3py dependency, and every phono3py release
# requires phonopy>=3.5 -- that is unsatisfiable against the phonopy pin
# above, and pip backtracks into an old phono3py sdist that fails to build.
mattersim==1.2.0  # 1.2.2 is the last phono3py-free release

#cuequivariance
#cuequivariance-torch
#cuequivariance-ops-torch-cu12
