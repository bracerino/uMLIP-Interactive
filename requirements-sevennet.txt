streamlit==1.57.0
plotly
pymatgen==2025.10.7
matscipy==1.2.0 # required for ASE 3.26
ase==3.28.0
#phonopy==2.40.0
phonopy==2.41.0
py3Dmol==2.5.0

psutil
gputil

seekpath==2.2.1

# SevenNet runs on PyTorch (shares the torch 2.8 stack with MACE / CHGNet /
# orb-models), so the CUDA build of torch is what puts it on the GPU.
torch==2.8.0
sevenn==0.13.0
